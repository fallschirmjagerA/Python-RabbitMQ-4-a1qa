# python-rabbitmq / script on virtual machine

1. docker-compose up -d
2. python3 -m venv .venv
# for linux
3. source .venv/bin/activate
4. pip install -r requirements.txt
5. python3 weather-worker.py
